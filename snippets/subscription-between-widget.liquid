<div class="flex flex-col w-full gap-0 max-w-[500px] mx-auto">
  <!-- Ships Every 3 Months Header -->
  <div class="sub-only-content text-center py-2">
    <span class="text-[15px] md:text-[16px] font-bold text-[#001C19]">Ships Every 3 Months</span>
  </div>

  <!-- Toggle Section -->
  <div class="flex items-center gap-6 justify-center text-center bg-[#E5F5F3] border border-[#00B7A2] rounded-[10px] py-4 px-4">
    <div class="text-[15px] md:text-[16px] font-normal !leading-tight text-[#001C19]">
      One-Time<br>Purchase
    </div>
    <div class="toggle sub-onetime-toggle group cursor-pointer select-none active">
      <div class="toggle-box w-16 h-8 rounded-full !block bg-gray-300 group-[.active]:bg-[#00B7A2] relative transition-all duration-300">
        <div class="toggle-knob w-7 h-7 bg-white rounded-full !block absolute top-0.5 left-0.5 transition-all duration-300 group-[.active]:translate-x-8"></div>
      </div>
    </div>
    <div class="text-[15px] md:text-[16px] font-bold !leading-tight text-[#001C19]">
      Subscribe<br>& Save 20%
    </div>
  </div>

  <!-- Simple Value Prop - Subscribe Only -->
  <div class="sub-only-content bg-[#E5F5F3] rounded-b-[10px] border border-t-0 border-[#00B7A2] mt-[-10px] pt-[18px] pb-3 px-4">
    <div class="flex flex-col gap-2 text-center">
      <div class="text-[14px] md:text-[15px] font-medium text-[#001C19]">
        ✓ Free fast shipping, always
      </div>
      <div class="text-[13px] text-[#666] leading-snug">
        Cancel anytime • Skip or pause whenever you want
      </div>
    </div>
  </div>

  <!-- Inline Mini FAQs -->
  <div class="sub-only-content flex flex-col gap-2 mt-4">
    <details class="group bg-white border border-[#e0e0e0] rounded-lg">
      <summary class="flex items-center justify-between cursor-pointer px-4 py-3 text-[14px] font-medium text-[#001C19] list-none">
        <span>What exactly am I paying for?</span>
        <svg class="w-4 h-4 text-[#00B7A2] transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
      </summary>
      <div class="px-4 pb-3 text-[13px] text-[#666] leading-relaxed">
        <strong>First order:</strong> You get the dispenser(s) + cleaning cartridges.<br>
        <strong>Every 3 months:</strong> We send refill cartridges only (at 20% off).
      </div>
    </details>

    <details class="group bg-white border border-[#e0e0e0] rounded-lg">
      <summary class="flex items-center justify-between cursor-pointer px-4 py-3 text-[14px] font-medium text-[#001C19] list-none">
        <span>Can I cancel anytime?</span>
        <svg class="w-4 h-4 text-[#00B7A2] transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
      </summary>
      <div class="px-4 pb-3 text-[13px] text-[#666] leading-relaxed">
        Yes! Cancel, skip, or pause anytime with one click. No commitments, no hassle.
      </div>
    </details>

    <details class="group bg-white border border-[#e0e0e0] rounded-lg">
      <summary class="flex items-center justify-between cursor-pointer px-4 py-3 text-[14px] font-medium text-[#001C19] list-none">
        <span>What if it doesn't fit my toilet?</span>
        <svg class="w-4 h-4 text-[#00B7A2] transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
      </summary>
      <div class="px-4 pb-3 text-[13px] text-[#666] leading-relaxed">
        TrueClean fits 95% of toilets. If yours is the exception, return it for a full refund within 90 days. No questions asked.
      </div>
    </details>
  </div>
</div>

<style>
  #productsVariants variant-selects + variant-selects {
    display: none !important;
    height: 0;
    opacity: 0!important;
    visibility: invisible!important;
  }
</style>

<script>
    const toggle = document.querySelector('.toggle');
    if (toggle) {
      // Get the elements to show/hide
      const subOnlyElements = document.querySelectorAll('.sub-only-content');

      toggle.addEventListener('click', function () {
        this.classList.toggle('active');
        const isActive = this.classList.contains('active');
        const productsVariants = document.querySelector('#productsVariants');

        if (!isActive) {
          // One-Time Purchase selected - HIDE subscription content
          const input = document.querySelector('input[re-sub-widget__selling-plan-input]');
          if (input) input.value = '';
          if (productsVariants) {
            productsVariants.classList.remove('show-subscription');
            productsVariants.classList.add('show-onetime');
          }

          // Hide subscription-only elements
          subOnlyElements.forEach(el => el.style.display = 'none');

        } else {
          // Subscribe & Save selected - SHOW subscription content
          const selectedLabel = document.querySelector(
            '.product-form__input--pill input[type=radio].new-lp-variant--inut-sub:checked+label.new-lp-variant--label-sub'
          );
          if (selectedLabel) {
            const subId = selectedLabel.getAttribute('data-subscription-id');
            const input = document.querySelector('input[re-sub-widget__selling-plan-input]');
            if (input) input.value = subId;
          }
          if (productsVariants) {
            productsVariants.classList.remove('show-onetime');
            productsVariants.classList.add('show-subscription');
          }

          // Show subscription-only elements
          subOnlyElements.forEach(el => el.style.display = '');
        }
      });
    }
  </script>
